a0 = int(input())
b0 = int(input())
a1 = []
b1 = []
#Представляем первое число в виде списка
while a0 > 0:
    a1.append(a0 % 10)
    a0 //= 10
#Представляем второе число в виде списка
while b0 > 0:
    b1.append(b0 % 10)
    b0 //= 10
#Цифры в обоих числах сейчас идут в обратном порядке - для удобства счета
sum = [] #В этой переменной будет записываться сумма двух чисел
k = 0 #Это та самая единичка, которую мы "держим в уме", когда складываем столбиком
#Проверяем, совпадают ли разряды чисел:
#Если они не совпадают,
#то припысываем к числу с меньшим разрядом нули слева до тех пор,
#пока длины списков не станут равными
if len(a1) > len(b1):
    while len(a1) > len(b1):
        b1.append(0)
elif len(a1) < len(b1):
    while len(a1) < len(b1):
        a1.append(0)
#Выполняем, собственно, само сложение:
for i in range(len(b1)):
        if a1[i] + b1[i] + k > 9:
            sum.append((a1[i] + b1[i] + k) % 10)
            k = 1
        else:
            sum.append((a1[i] + b1[i]) + k)
            k = 0
if k > 0:
    sum.append(k)
#Возвращаем сумму в правильном порядке:
sum.reverse()
print(sum)
